import React from 'react'
import styled from 'styled-components'
// components
// constants
import { WORKSPACE_GRID } from '../../vars/theme'

const Svg = styled.svg`
  width: 100%;
  height: 100%;
  background: ${WORKSPACE_GRID};
`

const Workspace = (props) => (
  <Svg>

  </Svg>
)

export default Workspace
