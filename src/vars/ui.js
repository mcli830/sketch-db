import { UNIT } from './theme'

export const START_MODE = 'nav'

export const WORKSPACE_SIZE = 3000

export const TAB_HEIGHT = UNIT * 4

export const CONTROL_WIDTH = UNIT * 8
export const CONTROL_WIDTH_FULL = CONTROL_WIDTH * 3.3

// export const FLOATING_INPUT_WIDTH = UNIT*8

export const TABLE_DIM = {
  padding: UNIT,
  width: UNIT*24,
  name: {
    fontSize: UNIT*3,
    lineHeight: UNIT*5,
  },
  field: {
    fontSize: UNIT*2,
    lineHeight: UNIT*3,
  }
}
